document.addEventListener('DOMContentLoaded',async()=>{const grid=document.querySelector('#property-grid');const filter=document.querySelector('#type-filter');let all=await getProperties(false);function render(){const val=filter?.value||'';const list=val?all.filter(p=>p.property_type===val):all;grid.innerHTML=list.length?list.map(propertyCard).join(''):'<div class="empty">Nenhum imóvel encontrado.</div>'}filter?.addEventListener('change',render);render();document.querySelectorAll('[data-wa]').forEach(a=>a.href=wa('Olá, Gideon! Vi seu site e quero conhecer as oportunidades disponíveis.'));});
